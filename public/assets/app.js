document.addEventListener('DOMContentLoaded',()=>{const dateButtons=[...document.querySelectorAll('.date-card')];dateButtons.forEach(btn=>btn.addEventListener('click',()=>{dateButtons.forEach(x=>x.classList.remove('selected'));btn.classList.add('selected');document.querySelectorAll('[data-slots-for]').forEach(g=>g.hidden=g.dataset.slotsFor!==btn.dataset.date)}));const modal=document.getElementById('confirmModal');const choice=document.getElementById('modalChoice');const slotInput=document.getElementById('modalSlotId');document.querySelectorAll('.slot').forEach(btn=>btn.addEventListener('click',()=>{if(!modal)return;slotInput.value=btn.dataset.slotId;choice.textContent=`${btn.dataset.dateLabel} às ${btn.dataset.time}`;modal.hidden=false;document.body.style.overflow='hidden'}));document.querySelectorAll('[data-close]').forEach(btn=>btn.addEventListener('click',()=>{if(!modal)return;modal.hidden=true;document.body.style.overflow=''}));modal?.addEventListener('click',e=>{if(e.target===modal){modal.hidden=true;document.body.style.overflow=''}});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&modal&&!modal.hidden){modal.hidden=true;document.body.style.overflow=''}})});
